/**
 * Twitter card image. Reuses the OpenGraph generator so the previews stay
 * in sync across X, LinkedIn, Slack, iMessage, etc.
 */

export { default } from "./opengraph-image";
export { runtime, alt, size, contentType } from "./opengraph-image";
